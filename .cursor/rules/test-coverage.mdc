---
description: All code must reach 80% test coverage (line and branch)
alwaysApply: true
---

# 80% code coverage — required

Every change ships with tests that bring the affected code to **≥ 80% coverage — both line and
branch.** No code is "done" until the threshold is met and CI is green.

## Rules

- Write tests **with** the code (same change/PR), not later. Cover every branch, error path, and
  edge case — not just the happy path.
- **CI enforces the threshold.** Coverage gates are set to 80% (line + branch); a drop below
  80% fails the build. Do not lower the threshold to make a build pass.
- Coverage tooling: **TypeScript** (`apps/web`, `apps/api`, `packages/shared-types`) and
  **Python** (`apps/rag`) runs coverage in CI.
- Test the module's **public surface** and its behavior, not implementation details.
- If a line is genuinely impossible to cover, justify it with an explicit, reviewed ignore
  annotation (e.g. `/* istanbul ignore next */`, `# pragma: no cover`) — used **rarely** and only
  with a one-line reason. Do not blanket-ignore files.
- 80% is a floor, not the goal: prefer meaningful assertions over tests written only to touch lines.

## Definition of Done (coverage part)

- [ ] New/changed code has tests; coverage stays at or above 80% (line + branch).
- [ ] Coverage report shows ≥ 80% for the touched packages; CI gate passes.
- [ ] Any `ignore`/`no cover` is justified inline and reviewed.

See `docs/development-workflow.md` §5.
